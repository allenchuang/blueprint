import { CardPreviewClient } from "@/components/marketing/card-preview-client";

export const metadata = {
  title: "Card Preview — Blueprint Admin",
};

export default function CardPreviewPage() {
  return <CardPreviewClient />;
}
