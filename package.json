{
  "name": "blueprint-stack",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "dev:web": "turbo run dev --filter=web",
    "dev:admin": "turbo run dev --filter=admin",
    "dev:server": "turbo run dev --filter=server",
    "dev:docs": "turbo run dev --filter=docs",
    "dev:remotion": "turbo run dev --filter=remotion",
    "dev:os": "turbo run dev --filter=os",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "db:generate": "turbo run db:generate",
    "db:migrate": "turbo run db:migrate",
    "db:push": "turbo run db:push",
    "db:studio": "turbo run db:studio",
    "sync-config": "tsx packages/app-config/scripts/sync.ts",
    "build:cli": "turbo run build --filter=blueprint-stack",
    "release:cli": "npm run release --prefix packages/blueprint-cli",
    "release:cli:minor": "cd packages/blueprint-cli && npm version minor --no-git-tag-version && bash release.sh --no-bump",
    "release:cli:major": "cd packages/blueprint-cli && npm version major --no-git-tag-version && bash release.sh --no-bump"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "tsx": "^4.19.0",
    "turbo": "^2.8.16",
    "typescript": "5.9.2"
  },
  "packageManager": "pnpm@10.0.0",
  "engines": {
    "node": ">=23"
  },
  "pnpm": {
    "overrides": {
      "@types/react": "^19.0.0",
      "@types/react-dom": "^19.0.0"
    }
  }
}
